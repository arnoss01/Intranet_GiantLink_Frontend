import { RequestRecuperationHoliday } from './request-recuperation-holiday.model';

describe('RequestRecuperationHoliday', () => {
  it('should create an instance', () => {
    expect(new RequestRecuperationHoliday()).toBeTruthy();
  });
});
